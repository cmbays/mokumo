# Rust
target/
*.rs.bk

# Node / SvelteKit
node_modules/
.svelte-kit/
apps/web/build/
.pnpm-store/
# Environment
.env
.env.*
!.env.example
.envrc
# SQLite
*.db
*.db-wal
*.db-shm
# Moon
.moon/cache/
# Claude Code
.claude/*
!.claude/rules/
!.claude/rules/**
!.claude/routines/
!.claude/routines/**
# OS
.DS_Store
Thumbs.db
# Claude Code session focus (ephemeral)
.claude/focus.md
# Pipeline state (ephemeral)
.workflow/
# IDE
.idea/
.vscode/
*.swp
*.swo
# Build artifacts
tmp/
dist/
# Test coverage
coverage/
coverage.json
coverage-branches.json
coverage-breakouts.json
lcov.info
# `coverage/` above matches both runtime artifacts AND the producer
# library module path. Re-include the source so the producer code itself
# stays in git (mokumo#583, see `tools/docs-gen/src/coverage/`).
!tools/docs-gen/src/coverage/
!tools/docs-gen/src/coverage/**
# Storybook
apps/web/storybook-static/
apps/web/.features-gen/
# Playwright
test-results/
apps/web/test-results/
# agent-browser dogfood artifacts (screenshots, repro videos, reports)
dogfood-output/
apps/web/playwright-report/
apps/web/.playwright-browsers/
# Lefthook personal overrides
lefthook-local.yml
# Pipeline state (transient, per-session)
# ts-rs default output (canonical path is apps/web/src/lib/types/ via Moon)
**/bindings/
# SQLx offline cache (.sqlx/) — commit after running: moon run shop:db-prepare
mutants.out/
mutants.out.old/
# ts-rs export artifacts in crate dirs
crates/types/apps/
# Vitest coverage
apps/web/coverage/
# Stryker mutation testing
**/reports/mutation/
**/.stryker-tmp/
# Firecrawl research cache
.firecrawl/
# Hurl env files (contain credentials) — commit *.env.example, not *.env
tests/api/envs/*.env
# Bruno collection (local exploration scratchpad — .hurl files are the committed contract)
tests/api/bruno/
pre-stage3/
