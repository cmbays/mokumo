{
  "name": "@mokumo/scorecard-renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Scorecard renderer + validator scripts invoked by .github/workflows/scorecard-comment.yml",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
