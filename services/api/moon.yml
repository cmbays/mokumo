language: rust
stack: backend
env:
  CARGO_TERM_COLOR: always
fileGroups:
  sources:
  - src/**/*
  - Cargo.toml
  - /Cargo.toml
  - /Cargo.lock
  migrations:
  - /crates/db/src/migration/**/*
  allRust:
  - /crates/*/src/**/*
  - /crates/*/Cargo.toml
  - src/**/*
  - Cargo.toml
  - /Cargo.toml
  - /Cargo.lock
tasks:
  build:
    command: cargo build --package mokumo-api
    deps:
    - web:build
    inputs:
    - '@group(allRust)'
    options:
      runFromWorkspaceRoot: true
  dev:
    # Build first, then exec the binary directly so Moon holds the actual server PID.
    # `cargo run` forks a child and doesn't forward signals — the binary becomes an
    # orphan when the parent is killed. `exec` replaces the shell with the server process.
    script: |
      set -euo pipefail
      cargo build --package mokumo-api
      TARGET_DIR=$(cargo metadata --no-deps --format-version 1 | python3 -c 'import sys,json;print(json.load(sys.stdin)["target_directory"])')
      exec "$TARGET_DIR/debug/mokumo-api"
    deps:
    - web:build
    options:
      cache: false
      persistent: true
      outputStyle: stream
      runFromWorkspaceRoot: true
  test:
    command: cargo nextest run --profile ci --workspace --exclude mokumo-desktop --exclude kikan-tauri
    deps:
    - web:build
    inputs:
    - '@group(allRust)'
    - /crates/*/tests/**/*
    - tests/**/*.rs
    options:
      runFromWorkspaceRoot: true
  test-bdd:
    command: cargo test --package mokumo-api --test bdd --features bdd
    deps:
    - web:build
    inputs:
    - '@group(allRust)'
    - tests/**/*.rs
    - tests/features/**/*.feature
    options:
      runFromWorkspaceRoot: true
  crap:
    # CRAP (Change Risk Anti-Patterns) quality gate — scoped to unit-testable crates.
    # Integration-only layers (services/api/src, apps/mokumo-desktop) are excluded via crap4rs.toml.
    # Generates LCOV coverage then runs crap4rs in one step.
    script: |
      set -euo pipefail
      mkdir -p tmp
      cargo llvm-cov nextest --workspace --exclude mokumo-desktop --exclude kikan-tauri --lcov --output-path tmp/lcov.info
      crap4rs --coverage tmp/lcov.info --src .
    deps:
    - web:build
    inputs:
    - '@group(allRust)'
    - /crap4rs.toml
    options:
      cache: false
      runFromWorkspaceRoot: true
  coverage:
    # --lib: unit tests only. Integration/BDD coverage excluded intentionally at M0.
    # Add --tests when domain logic in crates/core/ warrants full-stack coverage.
    command: cargo llvm-cov nextest --lib --workspace --exclude mokumo-desktop --exclude kikan-tauri --json --output-path coverage.json
    deps:
    - web:build
    inputs:
    - '@group(allRust)'
    options:
      cache: false
      runFromWorkspaceRoot: true
  coverage-report:
    # Standalone alternative to `coverage` — re-runs tests with instrumentation.
    # Keep command flags in sync with `coverage` above.
    command: cargo llvm-cov nextest --lib --workspace --exclude mokumo-desktop --exclude kikan-tauri --html --output-dir coverage/rust
    deps:
    - web:build
    inputs:
    - '@group(allRust)'
    options:
      cache: false
      runFromWorkspaceRoot: true
  lint:
    command: cargo clippy --workspace --exclude mokumo-desktop --exclude kikan-tauri --all-targets -- -D warnings
    deps:
    - web:build
    inputs:
    - '@group(allRust)'
    options:
      runFromWorkspaceRoot: true
  deny:
    command: cargo deny check
    inputs:
    - /deny.toml
    - /Cargo.lock
    options:
      runFromWorkspaceRoot: true
  fmt:
    command: cargo fmt --all --check
    inputs:
    - '@group(allRust)'
    options:
      runFromWorkspaceRoot: true
  fmt-write:
    command: cargo fmt --all
    options:
      cache: false
      runFromWorkspaceRoot: true
  gen-types:
    command: cargo test --package mokumo-types -- export_bindings
    env:
      TS_RS_EXPORT_DIR: apps/web/src/lib/types
    inputs:
    - /crates/types/src/**/*
    - /crates/types/Cargo.toml
    outputs:
    - /apps/web/src/lib/types/
    options:
      runFromWorkspaceRoot: true
  db-prepare:
    command: cargo sqlx prepare --workspace
    inputs:
    - '@group(allRust)'
    - '@group(migrations)'
    options:
      cache: false
      runFromWorkspaceRoot: true
  mutate:
    command: cargo mutants --workspace
    options:
      cache: false
      runFromWorkspaceRoot: true
  smoke:
    # External HTTP smoke tests against a running server.
    # Boots the server, runs .hurl files in tests/api/, then stops the server.
    # Requires: hurl CLI (brew install hurl).
    # Local run: moon run api:smoke
    # Env file: copy tests/api/envs/local.env.example → tests/api/envs/local.env (gitignored)
    # Note: authenticated-endpoint tests (list, create, get) are excluded here — they require
    # seed credentials. Add a separate api:smoke-authed task when demo seed data is stable.
    script: |
      set -euo pipefail
      cp -n tests/api/envs/ci.env.example tests/api/envs/ci.env 2>/dev/null || true
      cargo build --bin mokumo-api
      TARGET_DIR=$(cargo metadata --no-deps --format-version 1 | python3 -c 'import sys,json;print(json.load(sys.stdin)["target_directory"])')
      "$TARGET_DIR/debug/mokumo-api" &
      SERVER_PID=$!
      trap 'kill $SERVER_PID 2>/dev/null || true' EXIT
      for i in $(seq 1 30); do
        if curl -sf http://localhost:6565/api/health > /dev/null 2>&1; then break; fi
        sleep 1
      done
      # list-unauthenticated.hurl excluded: demo auto-login returns 200 locally.
      # That test is valid in CI where the server boots without demo mode.
      hurl --variables-file tests/api/envs/ci.env --test \
        tests/api/routing/api-prefix-boundary.hurl \
        tests/api/routing/method-rejection.hurl \
        tests/api/health/health.hurl \
        tests/api/offline/boot.hurl \
        tests/api/auth/login-bad-credentials.hurl \
        tests/api/auth/me-unauthenticated.hurl \
        tests/api/customers/not-found.hurl \
        tests/api/shop/logo-get.hurl
    deps:
    - web:build
    inputs:
    - '@group(allRust)'
    - tests/api/**/*.hurl
    - tests/api/envs/ci.env.example
    options:
      cache: false
      runFromWorkspaceRoot: true
  entity-check:
    command: cargo test --package mokumo-db --test entity_drift
    inputs:
    - '@group(allRust)'
    - '@group(migrations)'
    - /crates/*/tests/**/*
    options:
      runFromWorkspaceRoot: true
  bindings-check:
    deps:
    - web:build
    script: |
      set -euo pipefail
      TEMP_BINDINGS="$(mktemp -d)"
      trap 'rm -rf "$TEMP_BINDINGS"' EXIT

      TS_RS_EXPORT_DIR="$TEMP_BINDINGS" cargo test --workspace --exclude mokumo-desktop --exclude kikan-tauri

      # Format generated files so diff ignores stylistic differences (oxfmt vs ts-rs)
      npx oxfmt "$TEMP_BINDINGS"/*.ts 2>/dev/null || true
      npx oxfmt apps/web/src/lib/types/*.ts 2>/dev/null || true

      diff -ru apps/web/src/lib/types/ "$TEMP_BINDINGS/"
    inputs:
    - '@group(allRust)'
    options:
      runFromWorkspaceRoot: true
