language: 'javascript'
stack: 'frontend'

fileGroups:
  app:
    - 'static/**/*'
    - 'svelte.config.*'
    - 'vite.config.*'
  sources:
    - 'src/**/*'
  tests:
    - 'tests/**/*'
    - 'src/**/*.test.*'

tasks:
  dev:
    command: 'pnpm dev'
    options:
      cache: false
      persistent: true
      outputStyle: 'stream'

  build:
    command: 'pnpm build'
    inputs:
      - '@group(app)'
      - '@group(sources)'
      - 'package.json'
      - 'tsconfig.json'
    outputs:
      - 'build'

  preview:
    command: 'pnpm preview'
    deps:
      - 'build'
    options:
      cache: false
      persistent: true
      outputStyle: 'stream'

  test:
    command: 'pnpm test'
    inputs:
      - '@group(sources)'
      - '@group(tests)'

  check:
    command: 'pnpm check'
    inputs:
      - '@group(sources)'
      - 'tsconfig.json'
