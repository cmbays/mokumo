// Stage 0 stub — headless server binary.
// Axum data plane + kikan-socket daemon migrate here in Stages 6–7.
// This binary must never depend on Tauri. See ADR: adr-workspace-split-kikan (I3).
fn main() {}
